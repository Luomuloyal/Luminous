# Markdown 文档归档

这个目录用于统一存放仓库中的非 `README.md` Markdown 文档。

## 目录说明

- `root/`：原项目根目录下的说明类文档
- `lib-docs/`：原 `lib/docs/` 下的接口、部署与对齐文档
- `study/`：原 `Study/` 下的学习与排障文档

## 约定

- 各目录中的 `README.md` 保留在原位置，继续作为原目录入口说明。
- 新增非 README 的 Markdown 文档，优先按主题放到本目录对应子目录下。
